apiVersion: v1
kind: Service
metadata:
  name: payments-api
  namespace: fcg-payments
  labels:
    app: payments-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: payments-api
